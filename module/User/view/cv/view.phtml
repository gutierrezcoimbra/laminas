<?php
$title = 'CV: ' . $this->escapeHtml($cv->titulo);
$this->headTitle($title);
?>

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <div class="mb-3">
                <a href="<?= $this->url('cv', ['userId' => $cv->userId]) ?>" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Volver a CVs
                </a>
                <a href="<?= $this->url('user') ?>" class="btn btn-outline-secondary">
                    <i class="fas fa-users"></i> Todos los Usuarios
                </a>
            </div>
            <!-- Usar el ViewHelper -->
            <?= $this->viewCVHelper($cv->id) ?>

        </div>
    </div>
</div>

<style>
@media print {
    .btn, .mb-3 { display: none !important; }
    .card { border: none !important; box-shadow: none !important; }
}
</style>