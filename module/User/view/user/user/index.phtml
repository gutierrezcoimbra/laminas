<?php
/**
 * Vista para mostrar la lista de usuarios
 * @var array $users Lista de usuarios
 */
?>

<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <h1 class="mb-4">Lista de Usuarios</h1>
            
            <?= $this->partial('user/partials/flash-messages'); ?>
            
            <div class="mb-3">
                <a href="<?= $this->url('user/add') ?>" class="btn btn-success">
                    <i class="fas fa-plus"></i> Agregar Nuevo Usuario
                </a>
            </div>
            
            <div class="card modern-card">
                <div class="card-header modern-header">
                    <div class="d-flex justify-content-between align-items-center">
                        <h5 class="mb-0 text-white">
                            <i class="fas fa-users me-2"></i>
                            Usuarios Registrados (<?= count($users) ?>)
                        </h5>
                        <div class="header-actions">
                            <button class="btn btn-outline-light btn-sm me-2">
                                <i class="fas fa-filter"></i> Filtrar
                            </button>
                            <button class="btn btn-outline-light btn-sm">
                                <i class="fas fa-download"></i> Exportar
                            </button>
                        </div>
                    </div>
                </div>
                <div class="card-body p-0">
                    <?php if (empty($users)): ?>
                        <div class="empty-state">
                            <i class="fas fa-users fa-3x text-muted mb-3"></i>
                            <h5 class="text-muted">No hay usuarios registrados</h5>
                            <p class="text-muted">Comienza agregando tu primer usuario al sistema</p>
                        </div>
                    <?php else: ?>
                        <div class="modern-table-container">
                            <?php foreach ($users as $user): ?>
                                <div class="user-row <?= $user->isDeleted() ? 'user-deleted' : '' ?>">
                                    <div class="row align-items-center">
                                        <div class="col-md-1">
                                            <div class="user-avatar">
                                                <span class="avatar-text">
                                                    <?= strtoupper(substr($user->getNombre(), 0, 1) . substr($user->getApellidos(), 0, 1)) ?>
                                                </span>
                                            </div>
                                        </div>
                                        <div class="col-md-3">
                                            <div class="user-info">
                                                <h6 class="user-name mb-1"><?= $this->escapeHtml($user->getNombreCompleto()) ?></h6>
                                                <small class="user-id text-muted">ID: <?= $this->escapeHtml($user->getId()) ?></small>
                                            </div>
                                        </div>
                                        <div class="col-md-3">
                                            <div class="contact-info">
                                                <div class="email-info">
                                                    <i class="fas fa-envelope text-muted me-2"></i>
                                                    <?= $this->escapeHtml($user->getEmail()) ?>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-md-2">
                                            <div class="user-details">
                                                <?php if ($user->getFechaNacimiento()): ?>
                                                    <div class="detail-item">
                                                        <small class="text-muted">Nacimiento</small><br>
                                                        <span><?= $user->getFechaNacimiento()->format('d/m/Y') ?></span>
                                                    </div>
                                                <?php endif; ?>
                                                <?php if ($user->getEdad() !== null): ?>
                                                    <div class="age-badge">
                                                        <?= $user->getEdad() ?> años
                                                    </div>
                                                <?php endif; ?>
                                            </div>
                                        </div>
                                        <div class="col-md-2">
                                            <div class="status-section">
                                                <?php if ($user->isDeleted()): ?>
                                                    <span class="status-badge status-deleted">
                                                        <i class="fas fa-times-circle me-1"></i>Eliminado
                                                    </span>
                                                <?php else: ?>
                                                    <span class="status-badge status-active">
                                                        <i class="fas fa-check-circle me-1"></i>Activo
                                                    </span>
                                                <?php endif; ?>
                                            </div>
                                        </div>
                                        <div class="col-md-1">
                                            <div class="action-buttons">
                                                <div class="btn-group-vertical">
                                                    <a href="<?= $this->url('user/view', ['id' => $user->getId()]) ?>" 
                                                       class="action-btn view-btn" title="Ver perfil completo">
                                                        <i class="fas fa-eye"></i>
                                                    </a>
                                                    <?php if (!$user->isDeleted()): ?>
                                                        <a href="<?= $this->url('user/edit', ['id' => $user->getId()]) ?>" 
                                                           class="action-btn edit-btn" title="Editar usuario">
                                                            <i class="fas fa-edit"></i>
                                                        </a>
                                                        <a href="<?= $this->url('user/delete', ['id' => $user->getId()]) ?>" 
                                                           class="action-btn delete-btn" title="Eliminar usuario">
                                                            <i class="fas fa-trash"></i>
                                                        </a>
                                                    <?php endif; ?>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
/* Modern Card Styles */
.modern-card {
    border: none;
    border-radius: 15px;
    box-shadow: 0 8px 25px rgba(0,0,0,0.1);
    overflow: hidden;
}

.modern-header {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border: none;
    padding: 20px 25px;
}

.header-actions .btn {
    border-radius: 20px;
    font-size: 0.85rem;
    padding: 5px 15px;
}

/* Empty State */
.empty-state {
    text-align: center;
    padding: 60px 20px;
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
}

/* Modern Table Container */
.modern-table-container {
    background: #fff;
}

.user-row {
    padding: 20px 25px;
    border-bottom: 1px solid #f1f3f4;
    transition: all 0.3s ease;
    position: relative;
}

.user-row:hover {
    background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
    transform: translateX(5px);
    box-shadow: inset 4px 0 0 #667eea;
}

.user-row:last-child {
    border-bottom: none;
}

.user-deleted {
    background: rgba(220, 53, 69, 0.05);
    opacity: 0.7;
}

/* User Avatar */
.user-avatar {
    width: 45px;
    height: 45px;
    border-radius: 50%;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto;
}

.avatar-text {
    color: white;
    font-weight: 600;
    font-size: 0.9rem;
}

/* User Info */
.user-name {
    color: #2c3e50;
    font-weight: 600;
    margin-bottom: 0;
}

.user-id {
    font-size: 0.8rem;
    color: #6c757d;
}

/* Contact Info */
.email-info {
    color: #495057;
    font-size: 0.9rem;
}

/* User Details */
.detail-item {
    margin-bottom: 8px;
}

.detail-item small {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.age-badge {
    display: inline-block;
    background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
    color: white;
    padding: 4px 12px;
    border-radius: 15px;
    font-size: 0.8rem;
    font-weight: 500;
    margin-top: 5px;
}

/* Status Badges */
.status-badge {
    display: inline-block;
    padding: 6px 12px;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 500;
    text-align: center;
}

.status-active {
    background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
    color: white;
}

.status-deleted {
    background: linear-gradient(135deg, #dc3545 0%, #e85d75 100%);
    color: white;
}

/* Action Buttons */
.action-buttons {
    display: flex;
    flex-direction: column;
    gap: 5px;
    align-items: center;
}

.action-btn {
    width: 35px;
    height: 35px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    transition: all 0.3s ease;
    font-size: 0.85rem;
}

.view-btn {
    background: linear-gradient(135deg, #17a2b8 0%, #138496 100%);
    color: white;
}

.view-btn:hover {
    color: white;
    transform: scale(1.1);
    box-shadow: 0 5px 15px rgba(23, 162, 184, 0.4);
}

.edit-btn {
    background: linear-gradient(135deg, #ffc107 0%, #e0a800 100%);
    color: white;
}

.edit-btn:hover {
    color: white;
    transform: scale(1.1);
    box-shadow: 0 5px 15px rgba(255, 193, 7, 0.4);
}

.delete-btn {
    background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
    color: white;
}

.delete-btn:hover {
    color: white;
    transform: scale(1.1);
    box-shadow: 0 5px 15px rgba(220, 53, 69, 0.4);
}

/* Responsive Design */
@media (max-width: 768px) {
    .user-row {
        padding: 15px;
    }
    
    .modern-header {
        padding: 15px;
    }
    
    .header-actions {
        display: none;
    }
    
    .user-row .row > div {
        margin-bottom: 10px;
    }
    
    .action-buttons {
        flex-direction: row;
        justify-content: center;
    }
}
</style> 