<?php
$title = 'Perfil de ' . $this->escapeHtml($user->getNombreCompleto());
$this->headTitle($title);
?>

<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="mb-3">
                <a href="<?= $this->url('user') ?>" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Volver a Usuarios
                </a>
                <?php if (!$user->isDeleted()): ?>
                    <a href="<?= $this->url('user/edit', ['id' => $user->getId()]) ?>" class="btn btn-warning">
                        <i class="fas fa-edit"></i> Editar Usuario
                    </a>
                <?php endif; ?>
            </div>

            <!-- Información del Usuario -->
            <div class="card mb-4">
                <div class="card-header bg-primary text-white">
                    <h1 class="mb-0">
                        <i class="fas fa-user"></i> 
                        <?= $this->escapeHtml($user->getNombreCompleto()) ?>
                    </h1>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <h5><i class="fas fa-id-card"></i> Información Personal</h5>
                            <table class="table table-borderless">
                                <tr>
                                    <td><strong>ID:</strong></td>
                                    <td><?= $this->escapeHtml($user->getId()) ?></td>
                                </tr>
                                <tr>
                                    <td><strong>Nombre:</strong></td>
                                    <td><?= $this->escapeHtml($user->getNombre()) ?></td>
                                </tr>
                                <tr>
                                    <td><strong>Apellidos:</strong></td>
                                    <td><?= $this->escapeHtml($user->getApellidos()) ?></td>
                                </tr>
                                <tr>
                                    <td><strong>Email:</strong></td>
                                    <td><?= $this->escapeHtml($user->getEmail()) ?></td>
                                </tr>
                            </table>
                        </div>
                        <div class="col-md-6">
                            <h5><i class="fas fa-calendar-alt"></i> Datos Adicionales</h5>
                            <table class="table table-borderless">
                                <tr>
                                    <td><strong>Fecha de Nacimiento:</strong></td>
                                    <td>
                                        <?php if ($user->getFechaNacimiento()): ?>
                                            <?= $this->escapeHtml($user->getFechaNacimiento()->format('d/m/Y')) ?>
                                        <?php else: ?>
                                            <span class="text-muted">No especificada</span>
                                        <?php endif; ?>
                                    </td>
                                </tr>
                                <tr>
                                    <td><strong>Edad:</strong></td>
                                    <td>
                                        <?php if ($user->getEdad() !== null): ?>
                                            <?= $this->escapeHtml($user->getEdad()) ?> años
                                        <?php else: ?>
                                            <span class="text-muted">No calculable</span>
                                        <?php endif; ?>
                                    </td>
                                </tr>
                                <tr>
                                    <td><strong>Estado:</strong></td>
                                    <td>
                                        <?php if ($user->isDeleted()): ?>
                                            <span class="badge bg-danger">Eliminado</span>
                                        <?php else: ?>
                                            <span class="badge bg-success">Activo</span>
                                        <?php endif; ?>
                                    </td>
                                </tr>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

            <!-- CVs del Usuario -->
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h3 class="mb-0">
                        <i class="fas fa-file-text"></i> 
                        Currículums (<?= count($cvs) ?>)
                    </h3>
                </div>
                <div class="card-body">
                    <?php if (count($cvs) > 0): ?>
                        <div class="row">
                            <?php foreach ($cvs as $cv): ?>
                                <div class="col-md-6 mb-3">
                                    <div class="card h-100">
                                        <div class="card-header">
                                            <h5 class="card-title mb-0">
                                                <?= $this->escapeHtml($cv->getTitulo()) ?>
                                            </h5>
                                        </div>
                                        <div class="card-body">
                                            <?php if ($cv->getResumen()): ?>
                                                <p class="card-text">
                                                    <?= $this->escapeHtml(substr($cv->getResumen(), 0, 100)) ?>
                                                    <?= strlen($cv->getResumen() ?? '') > 100 ? '...' : '' ?>
                                                </p>
                                            <?php endif; ?>
                                            
                                            <div class="text-muted small mb-3">
                                                <i class="fas fa-calendar"></i>
                                                Creado: <?= $cv->getCreatedAt() ? $cv->getCreatedAt()->format('d/m/Y H:i') : 'N/A' ?>
                                            </div>
                                        </div>
                                        <div class="card-footer">
                                            <a href="<?= $this->url('cvs/view', ['id' => $cv->getId()]) ?>" 
                                               class="btn btn-primary btn-sm">
                                                <i class="fas fa-eye"></i> Ver CV Completo
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    <?php else: ?>
                        <div class="alert alert-info">
                            <h5><i class="fas fa-info-circle"></i> Sin CVs</h5>
                            <p class="mb-0">Este usuario no tiene currículums registrados.</p>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>   